# LotTicket

This file intentionally has no independent workflow rules.

The complete repository specification lives in `CLAUDE.md` at the repository
root. Read it before doing any work. Its invariants, verification requirements,
secret-handling rules, and model-specialized Codex workflow apply regardless of
which assistant is operating in this repository.

Do not copy the specification into this file. Keeping one source of truth
prevents the Codex and Claude instructions from drifting apart.
